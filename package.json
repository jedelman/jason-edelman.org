{
  "name": "jason-edelman-org",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "node scripts/build.mjs",
    "deploy": "npm run build && wrangler deploy --config wrangler.toml",
    "install-hooks": "sh scripts/install-hooks.sh",
    "check": "node scripts/pre-commit.mjs"
  },
  "devDependencies": {
    "wrangler": "^4.90.1"
  }
}
