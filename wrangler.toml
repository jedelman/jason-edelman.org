name = "jason-edelman-org"
compatibility_date = "2026-04-28"
account_id = "543fde7feabd0af945bfae926ceea7ad"

# public/ is the assets root — built by npm run build, not committed to git
[assets]
directory = "public"
not_found_handling = "single-page-application"

# Custom domain bindings are managed in the Cloudflare dashboard.
# Do NOT declare [[routes]] here — wrangler rewrites them on every deploy
# and has historically broken the custom domain binding.
